#include <cstddef> // size_t

#include <cstring> // strlen, strcpy
/* В этой задаче вам требуется реализовать конструктор, который принимает на вход C-style строку, вычисляет ее размер (без учета завершающего 0 символа) и сохраняет его в поле size*/
/*Карандашев П1-17*/
/*https://stepik.org/lesson/546/step/10?auth=registration&unit=869*/


struct String {



    /* Реализуйте этот конструктор */

	String(const char *str = "")

    {

        this->size = (strlen(str));

        this->str = (new char[strlen(str)+1]);

        for(int i=0; i<this->size; i++)

            this->str[i]=str[i];

        this->str[size] = '\0';

    }



	size_t size;

	char *str;

};
