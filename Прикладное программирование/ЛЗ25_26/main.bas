Attribute VB_Name = "main"
Public Num As Long, dish() As Блюда

Sub start()

'  Call init
 '  Call читать
 '  Call форма_init
   With Worksheets("Меню заведения")
        .Cells(2, 1).Select
   End With

End Sub

