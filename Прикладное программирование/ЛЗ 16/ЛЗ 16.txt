МДК.01.02 "Прикладное программирование"
Лабораторное занятие 16. Разработка программ с перегрузкой
Составитель: Гусятинер Л.Б., 16.05.2020, МГОТУ ККМТ, П1-17, П2-17
Выполнила: Саполетова Анастасия П1-17

Задания.
Задание 1. [2]Упражнение 7.34. Определите набор перегруженных функций по имени error, который соответствует 
следующим обращениям.
int index, upperBound; char selectVal;

void error (string,int,int);
void error (string);
void error (string, char);

error("Subscript out of bounds: ", index, upperBound);
error("Division by zero");
error("Invalid selection", selectVal);


Задание 2. [2]Упражнение 7.35. Объясните результат второго объявления в каждом из следующих наборов. 
Укажите, какое из них (если есть) недопустимо.

(a)	int calc(int, int);
	int calc(const int, const int); передаются константные переменные, результат будет одинаковый

(b)	int get();
        double get(); вариант недопустим, потому что эти две функции одинаковые и аргументы здесь не передаются и таким образом
		      будет ошибка

(c)	int *reset(int *); 
	double *reset(double *); передается и возвращается указатель на тип double




Задание 3. 
Разработать перегруженные функции для математических операций sum, div, prod, my_abs,
eq (equal), gt(greater then) 
1. для дробей вида a/b, где a,b - целые, b != 0
2. для комплексных чисел
3. для многочленов вида a[n]*x^n + a[n-1]*x^(n-1) + ... + a[1]*x + a[0]


Пункт 2 - выполнено